# Wimblo — complete report extraction

September 13, 2026. Continuation of option 2, with eligibility and Render deferred. Local synthetic engineering checkpoint; no buyer data or external provider execution.

The report screen offers **Export complete CSV** separately from **Export visible CSV**. Complete export downloads every result row of the reviewed report, with full saved source text, rather than only the current display page. Grouped reports export all groups and calculations; they do not silently replace grouped results with raw source records. Run an ungrouped report when individual records are needed. This is Excel-compatible CSV, not a native workbook or evidence that Excel has preserved every identifier and large numeric value.

Before export, the server rebuilds the selected report corpus and verifies the previous source fingerprint. The fingerprint includes the normalized reviewed definition and current user, role and tenant scope. Changed source facts or definition reject the export; the screen requires rerunning. The export concerns the reviewed result definition, which may be unsaved; it does not claim to represent the latest version of a separately edited saved report.

Complete export preserves the existing source allowlist, staff/administrator permissions and complete matched current/historical protected provenance checks. Original source custody is not changed. A pending response cannot initiate a browser download after editing, account/API changes or unmounting. No-write read authorization and CSRF checks apply to the export endpoint. It sends no data to providers and logs no exported payload.

Financial exports retain the chosen report semantics. Unfiltered gift rows can include future-dated gifts, fees and in-kind support; receipt/history/commitment values can repeat source references and are not additional income. Review scope and filters before reconciliation. The CSV contains column headers and results, not a standalone financial-policy declaration.

Money is serialized directly from integer cents using integer arithmetic, including signed amounts, with explicit USD column labels. CSV uses UTF-8 BOM, CRLF and quoted escaped fields. Text and labels that could be interpreted as spreadsheet formulas receive a protective prefix; signed numeric values remain numeric. Complete export does not shorten display-limited text. Oversize output is refused before download, with guidance to narrow the report; no partial file is labelled complete.

Limits remain explicit: 100,000 selected source rows, 64 MB projected source facts, 8 MB CSV and 12 MB complete response including privacy metadata. Ordinary report pages remain limited to 250 rows and the earlier 1 MB result/provenance budget. Native source lists are still materialized before these processing guards; this checkpoint is not a streaming ten-year archive export or production concurrency guarantee. Field discovery still samples 100 records. Buyer full field/history inventory, source reconciliation, real volume, Excel/PDF fidelity and institutional acceptance remain open.

## Verification

**918/918 integrated tests pass**, zero failures, cancellations or skips (77.603869s). Build passes (1.28s). Ten new backend cases and 92 related regressions pass; twelve new UI cases plus twelve scale regressions pass. Built native desktop (1440 × 1000) and mobile (390 × 844) journey downloads all 301 synthetic gift rows, including the undisplayed final record, exact $100.01 values and formula-protected text. Changed donor facts after the report reject the next download with expected HTTP 409 and require rerun. Page identity, nonblank content, no framework overlay, signed-in console health, screenshots and interaction checks pass. Initial HTTP 401 and deliberately induced stale HTTP 409 are expected. Browser plugin was unavailable; bundled Playwright was used. Source/evidence manifest: `1ec56dfacf097e2d19150c912fd0ecea78aa18cb8361b5686c86b84195f31108`, in `COMPLETE-REPORT-EXPORT-VERIFICATION.json`.

Requirements B2 and B6 describe the new complete extraction behavior while preserving their existing dispositions. Overall status remains 46 Local basic, 33 Partial, 2 Missing and 3 external evidence; all 84 criteria remain unaccepted by the buyer. Live email/bulk synchronization, buyer conversion and operated hosting/recovery remain separate workstreams.
