# Wimblo — option 2 core engineering checkpoint

September 13, 2026. Kode Kinetics LLC. Local synthetic source verification; no deployment, buyer acceptance or completed production handover.

The owner selected core-product work now and postponed eligibility discussion. Render remains deferred. This checkpoint closes specific import, reporting and controlled synchronization defects while recording operational dependencies honestly. The requirements register remains **46 Local basic / 33 Partial / 2 Missing / 3 external evidence, zero buyer accepted** across 84 criteria and 3,500 technical points. These counts do not predict evaluation points.

## Delivered and verified locally

| Area | Concrete new behavior and evidence | Material remaining work |
|---|---|---|
| Guided import | Larger CSVs become deterministic dependency-ordered reviewed parts within the existing 500-row / 2 MiB native transaction limit. Explicit constituent preferences are required. Parent records precede children. Stable source/key/content custody enables exact replay after restart or uncertain responses. Native synthetic verification imports **6,515 constituents and 1,622 designations through 18 parts**, checks contacts/preferences/parents/account codes, replays without duplicate records/maps/batches/audit, and verifies rollback/retry of a failed part. | Actual post-award buyer exports; complete entity, historical void/reversal/file mappings; duplicate policy; trial/delta/final cutover; independently signed counts/amounts/fields. The complete queue is not one atomic transaction. |
| Retained source controls | Administrator-only namespace reconciliation checks mapped targets against current saved rows; missing, changed or duplicate targets withhold certified controls. Digests/counts, designation codes and gift/allocation cents are retained. Five synthetic gift examples independently reconcile **43,003 cents**; they are not ten years of production receipts. UI clears old totals when another part starts and discards late earlier responses. | A namespace check cannot prove the entire buyer export was supplied. Actual full inventory and source-to-target acceptance remain required. |
| Reporting | Complete eligible selected-source matches and exact-cent calculations precede detail/group paging. Tests include a late match among 10,001 gifts, 20,002 split allocation rows, 301-row pages, source/definition/role/tenant change rejection and all-matched historical privacy. Fingerprints prevent combining pages from different source facts. | Accepted complete field inventory, rare optional fields, full-detail extraction policy and actual ten-year performance. Native lists materialize before engine limits; this is not an operated memory/load guarantee. |
| Report output/privacy | UI provides Next/Previous and explicitly page-specific CSV. Saved financial filters retain exact dollar/cents roundtrip. Scheduled snapshots retain complete counts/calculations and all-matched privacy references while storing the first detail page. Newly private evidence beyond that page denies retained output. Oversized complete output fails explicitly rather than dropping provenance. | Full-detail export/Excel/PDF acceptance and hosted schedule uptime/support. |
| Existing-member contact sync | Separate TEST_ONLY reviewed name/phone/type text-field PATCH or unsubscribe-only workflow. Native source, current primary-email binding, actor/factor/policy/recovery authority and exact request UUID are pinned. Durable write reservation precedes PATCH; exact readback governs completion. Both modules honor sticky marketing-only denial. Unknown/Executing and attempted-but-unresolved intents block a new UUID bypass; GET-only reconciliation never repeats PATCH. Native HTTP and UI regression tests verify permissions, rollback, concurrency, source changes and custody. | **Production integration is blocked.** No campaign sending, new member creation, email change, resubscription, consent grant or bulk synchronization. Old lifetime binding limit is still 1,000; 20,000 new intents do not support the buyer's roughly 6,500-contact integration. Changed-authority stale intents fail closed; their operator disposition/remediation workflow remains open. Actual account, sender, mapping, inbound lifecycle, residency and volume acceptance are pending. |
| Recovery operations tooling | Private Daily/Weekly operator-invoked offline schedule journal reserves an occurrence before archive work, verifies encrypted recovery into a new root, records immutable outcomes and blocks ambiguous cycles. Explicit reasoned recovery uses the same archive; interrupted publication requires an exact retained private object version. Two-worker reservation, deferred-commit rollback and synthetic off-host readback tests pass. | No daemon, maintenance scheduler, provider store or hosted cadence installed. Each invocation requires confirmation all app processes/workers are stopped; do not run it as an online backup. Actual private off-host storage, key/journal custody, monitored cadence, timed buyer-volume restore, RPO/RTO, retention and support remain unproved. |

## Explicit bounds

Guided preparation: 10 MiB per CSV, at most 100,000 staged rows and 2,000 prepared parts; each native part remains at most 500 rows, 10 files and 2 MiB. Re-uploading the identical original files, source, key and mappings reconstructs the same part identities. Ending preparation does not undo already committed parts. Staged browser data is not persisted to local storage.

Reports: selected raw metadata/required-history acquisition rejects above 100,000 rows; projected matching facts reject above 100,000 rows or 64 MB. Complete result/provenance rejects above 1 MB. Detail pages are at most 250 rows. Metadata catalog discovery samples 100 records and says so; it does not certify every optional field. The 100,001-row rejection test is not proof of buyer acceptance at 100,000 rows.

Contact sync: existing verified primary-email member only, reviewed allowlisted text fields at most 255 UTF-8 bytes each. Provider unsubscribed/cleaned/pending/transactional states do not receive additional fields or subscriptions. Unsubscribe denial never rewrites broad native Print/Post/manual correspondence preferences. An uncertain remote completion can remain unresolved; no atomic transaction spans the remote provider and SQLite.

Recovery journal: private mode 0600, separate from source/archive trees and **not included in the platform bundle**; retain its custody independently. Capacity: 5,000 runs / 50,000 immutable outcomes, latest 100 visible, at most 100 outcomes per occurrence. Capacity test seeds 4,999 synthetic history rows and performs one actual new drill; it is not 5,000 completed restores. Existing full-platform archive bounds remain 128 MiB and 1,000 tenants. Local Verified always reports offHostVerified=false.

## Independent testing

Native built browser fixture: `http://127.0.0.1:4345/`, local synthetic data and injected synthetic contact adapter only. It does not reflect public Vercel or Render readiness.

Verified screen journeys: a 501-profile CSV exceeding the single-batch file limit, individual preview/confirmation/save for both parts, retained namespace reconciliation, report Next/Previous with 250-row pages, existing-member observation→reviewed preparation→one application→retained history. Desktop 1440 px and mobile 390 px are checked; mobile document width is 390 px. The initial unauthenticated session request produces an expected 401; checked signed-in journeys have no browser runtime/console errors. Screenshots and machine-readable browser evidence are retained with the handover checkpoint.

Final integrated source: **835/835 tests pass**, zero failures/skips, **54.402263041 seconds**. Final production build passes in **1.52 seconds** (main bundle 495.79 kB). Final built native screen journeys pass on desktop/mobile. Source SHA-256 manifest and test/build log hashes are recorded in `CORE-CLOSURE-VERIFICATION.json`; this is uncommitted working source based on `8dfdc2870539a252819a4eabedd26467cabdffa4`, with no hosted rollout.

## Remaining closure order

1. Reliable early email: agreed provider/sender and actual account, complete current-contact lifecycle and suppression mapping, safe bulk scale, sending/failure/retry/delivery acceptance. The new existing-member slice is a foundation for this work, not Q&A53 completion.
2. Complete source conversion: accepted full field/history inventory and post-award trial/delta/final reconciliation. No actual buyer data has been requested or converted before award.
3. Operated hosting/recovery/security: country-approved durable business persistence, encrypted stored copies, adopted keys/journal custody, scheduled private off-host copies, monitoring, timed restore and attended support.
4. Adopted commercial/privacy/support evidence: signed DPA/data schedules, institutional approval, CIS/equivalent assurance, notices and accountable service staffing. Eligibility is deliberately postponed by the owner, not waived.

Preferred SSO remains separate from all-user MFA. Neon attached to Vercel is not a completed migration of SQLite business persistence. Optional model intelligence still has a previously reproduced whole-corpus stale-response defect; do not enable it for production until repaired and accepted. No superiority over competitor production platforms or competitive awarded score is claimed.

Provider references used for the bounded adapter contract: [official existing-member update API](https://mailchimp.com/developer/marketing/api/list-members/update-list-member/) and [official merge-field documentation](https://mailchimp.com/developer/marketing/docs/merge-fields/). These API capabilities do not establish account configuration, consent or production acceptance.
