# Wimblo — prospective communications workflows

September13,2026. Kode Kinetics LLC. Verified local source; no production deployment or buyer acceptance is claimed.

The final integrated application passes **644/644 tests**, zero failures or skips, in **29.93 seconds**. The final build passes in **1.24 seconds**, main bundle492.70kB. The first integrated run passed642/644; a grouped-report test assumed random-ID ordering and a UI test replaced its API after capturing a form handler. Both test defects were repaired; the final run above passed. Earlier releases are historical.

## What staff can do

Choose an existing active saved audience, a saved Messaging template and Print/Email-draft format. Review the current source baseline and selected wording, explicitly confirm prospective draft creation, then activate. Existing eligible profiles create **no historical drafts**. Future observed outside-to-eligible membership entries can create one native Communications Draft per episode. Ordinary profile edits do not repeat an eligible episode; an observed departure and re-entry can produce another. Changes entirely between polls are not observed.

Open retained execution history and follow its trusted Open draft action. The screen refreshes authorized workspace records before opening a background-produced draft. Every generated draft says **Not sent; human review required**. Current-source status is checked when opening/reporting. A retired/changed activation or source leaves historical wording readable under ordinary Communications permissions but explicitly requires current-source review. Known protected workflow drafts cannot be deleted through the displayed action or API. Reasoned terminal retirement preserves originals and execution history; no silent resume/edit/re-target occurs.

## Reliability, authorization and business rules

Activation/retirement require current tenant, account, session, role, CSRF and MFA policy. Background execution uses the explicitly reviewed owner's durable current account/role/MFA enrollment binding, not a live browser cookie. Logout, normal idle expiration and ordinary authenticator/recovery-code sign-in do not silently kill or restart the rule. Disabled/changed owner access, changed MFA enrollment or unavailable required protection closes it.

Audience criteria never override Do not contact. Email drafts additionally require Email preference and a valid primary email; secondary contacts are not additional recipients. Fresh eligibility, selected audience/template revisions, owner protection and the complete saved native draft are rechecked before transaction commit. No gift, income, soft credit, receipt, acknowledgment completion or provider send is created.

Watermarks, entry episodes, unique execution, native Draft and audit evidence commit transactionally. Independent genuine two-worker SQLite races, deferred commit/audit faults, poisoned saved-record metadata and consent withdrawal during draft audit are tested. Failures cannot report committed success. Durable retries use60-second delays, at most five persisted attempts, then terminal suppression. A full storage outage cannot truthfully persist failure/success history; no successful draft is asserted.

Platform suspension commits the registry block first. Pending workflow/reminder suppression and session revocation follow; partial failure keeps access suspended, repeat suspension retries effects, and resume performs suppression before admitting access. This is safe sequencing across two databases, not distributed atomicity.

## Recovery and reporting

Encrypted authenticated new-path recovery verifies original native history/schema, removes old sessions/challenges and appends exactly one immutable recovery marker if the source workspace contains the workflow marker schema. Marker instrumentation is a deliberate extra restored row, separate from original business/audit history. Exact prior marker rows remain unchanged; marker-trigger corruption of financial history rejects restore and publishes no destination. Pre-workflow archives preserve their original shape without inventing a marker table.

Restored old activations detect the changed marker and suppress before producing more drafts. A new reviewed activation is required; ordinary server restart keeps the unchanged marker and durable duplicate/retry protection. Institutional backup custody, source scale, approved retention/disposal and prevention of disposed data reappearing remain separate open obligations.

Five curated workflow rule/membership/entry/execution/outcome report sources are administrator-only and omit email, body, bindings, snapshots, raw digests and provider evidence. Native Communications reports retain ordinary authorized historical wording with optional trusted workflow-origin fields. SourceCurrent is a dated execution-time check; a retained scheduled report is not a live assurance. Exact native recovery, viewer/staff denial of administrator operational sources, account downgrade denial of retained scheduled output and current-to-opt-out native Draft CSV reporting are tested.

## Browser evidence and limits

The built isolated synthetic workspace completed: review22 source profiles →15 eligible baseline /16 matching /7 excluded →explicit activation with one Armed outcome and no historical draft →create Cory Workflow Synthetic with Annual donor + Email preference →one Drafted outcome at18:55:02.647Z →Open draft with exact rendered name/current-source guidance →later inspection still only one Drafted outcome →reasoned Retired v2 at18:56:56.296Z →retained original Draft with source-changed/human-review guidance. Final history has3 outcomes, not duplicate production. No external message was sent.

The390px mobile page has390px client/scroll width; selects318px, both320px table containers scroll their580px contents internally. The viewport was restored. Desktop/mobile inspection also repaired parent rerenders invalidating a current baseline/register refresh and refreshed missing background-produced records before navigation.

Focused new coverage: backend6 tests, independent integrity11, reporting6, UI6, recovery-marker3 =32 additional tests over the612-test contract-conversion baseline. Prior reporting/C2/reminder focused regression set52/52 passed.

Bounds:20 active rules/workspace;1,000 current/lifetime retained profile watermarks and1,000 entry episodes per activation;40 draft attempts per cycle;30-second worker; latest100 rules with active rules prioritized and latest100 outcomes per selected rule. This is not accepted proof at6,500 contacts or ten years of real history. Provider delivery, external consent synchronization, multi-step recurring sequences, historical marketing conversion, adopted institutional operations and buyer workflow acceptance remain pending.

**A7.3 is Partial**, not complete. Current84-row inventory: **46 local basic /26 partial /9 missing /3 external evidence**,3500 listed technical points, **zero buyer-accepted criteria**. Provider qualification/submission gates remain additional. The literal communication-workflow criterion and our engineering closure targets are distinguished; additional maturity goals are not falsely attributed to the buyer.
